# Public canonical origin; replace before publishing
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# local SQLite for local preview; neon for serverless production
STORAGE_DRIVER=local
DATABASE_URL=
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
RATE_LIMIT_SECRET=
# Set true only behind Vercel's trusted proxy
TRUST_PROXY=false
HOSTING_PLATFORM=
# Optional: external PDF in public/; configured in lib/content.ts
