# گزارش بررسی نسخه تحویلی

تاریخ بررسی: ۲۰۲۶/۰۹/۱۶. محیط: Windows، Node.js 24.17، Next.js 16.3.5. این گزارش درباره خروجی محلی است؛ سایت هنوز روی حساب میزبانی عمومی منتشر نشده است.

## نتیجه آزمون‌های انجام‌شده

| بررسی | نتیجه |
| --- | --- |
| ساخت Production با Next.js | موفق؛ ۲۷ مسیر تولید/ثبت‌شده |
| بررسی TypeScript | بدون خطا |
| هفت آزمون منطق و ذخیره‌سازی | همگی موفق |
| درخواست HTTP به ۱۷ مسیر صفحه، فایل و SEO | پاسخ ۲۰۰ |
| مسیر ناشناخته | پاسخ واقعی ۴۰۴ |
| فرم دو مرحله‌ای از مرورگر | ثبت واقعی در SQLite و کد پیگیری |
| تکرار درخواست با همان شناسه | همان کد پیگیری، بدون رکورد اضافه |
| رضایت نامعتبر و Honeypot | رد درخواست |
| مبدأ خارجی و Content-Type نامعتبر | پاسخ ۴۰۳ و ۴۱۵ |
| Rate Limit هم‌زمان | شمارش پایدار؛ فقط تعداد مجاز پذیرفته شد |
| تبدیل ارقام فارسی و شماره بین‌المللی | موفق |
| لینک مرجع با javascript یا file | رد اعتبارسنجی |
| دسته محصول در گالری | چهار اثر مطابق داده‌ها |
| Lightbox، اثر بعدی و Escape | موفق |
| WebMCP فیلتر | ثبت ابزار، فیلتر معتبر و رد دسته نامعتبر آزموده شد |
| تغییر دسته تعرفه | به‌روزرسانی پلن‌ها در مرورگر |
| پاسخ شناخته‌شده دستیار | پاسخ از FAQ |
| پاسخ نامشخص دستیار | اعلام نداشتن پاسخ قطعی و راه‌های تماس |
| منوی موبایل و Escape | موفق |
| PDF تعرفه | تولید، رندر و بازبینی بصری؛ لینک فایل پاسخ ۲۰۰ |
| وابستگی‌های زمان اجرا | npm audit در زمان بررسی: آسیب‌پذیری گزارش‌شده صفر |

## Responsive و دسترسی‌پذیری

چیدمان در عرض‌های ۳۶۰، ۳۹۰، ۷۶۸ و ۱۴۴۰ پیکسل بررسی شد. در اندازه‌های بررسی‌شده اسکرول افقی صفحه مشاهده نشد. ظاهر Hero در دسکتاپ و موبایل و صفحه تعرفه در موبایل کوچک بازبینی شد. فرم تمام فیلدهای برچسب‌دار دارد، تصاویر alt دارند، دکمه بی‌نام در بررسی DOM وجود نداشت و ساختار lang=fa / dir=rtl تأیید شد. Lightbox و منو از dialog بومی با مدیریت فوکوس و Escape استفاده می‌کنند. Focus style مشخص و Reduce Motion در CSS پیاده شده‌اند.

این بررسی معادل ممیزی جامع WCAG یا آزمون همه مرورگرها و صفحه‌خوان‌ها نیست. آزمون دستی VoiceOver/NVDA و بزرگ‌نمایی‌های مختلف برای انتشار عمومی گسترده توصیه می‌شود.

## Performance

تصاویر ارسالی به WebP تبدیل شدند. برای هر فایل، پنج اندازه از thumbnail تا Lightbox هنگام build ساخته می‌شود و مرورگر از srcset استفاده می‌کند. Hero اولویت بارگذاری دارد؛ تصاویر گالری lazy-load هستند و ابعاد رزروشده دارند. فونت محلی است و برای نمایش صفحه تماس خارجی لازم ندارد. APIهای فرم cache نمی‌شوند. صفحه آفلاین فقط برای اطلاع‌رسانی است و اطلاعات فرم را ذخیره نمی‌کند.

اندازه‌گیری میدانی LCP، INP و CLS و امتیاز PageSpeed روی دامنه نهایی هنوز انجام نشده است. عدد ساختگی برای سرعت یا Core Web Vitals گزارش نمی‌شود.

## موارد نیازمند تنظیم مالک

- اتصال GitHub و هاست و تعیین دامنه نهایی؛ هیچ Repository آنلاین یا URL عمومی ساخته نشده است.
- ایجاد Neon، اجرای schema و تنظیم Secrets در هاست؛ اتصال ابری به دلیل نبود حساب/اطلاعات اتصال آزموده نشده است.
- پیکربندی Cloudflare و OpenNext اضافه شده، اما build این مسیر در sandbox ویندوز با خطای esbuild هنگام خواندن مسیر تنظیمات متوقف شد. انتشار و اجرای Worker تأیید نشده؛ build اصلی Next.js موفق است. برای Cloudflare باید build در Workers Builds یا Linux و سپس آزمون فرم واقعی انجام شود.
- انتخاب پلن مناسب: Vercel Hobby برای استفاده غیرتجاری است؛ رایگان‌بودن آن برای جذب مشتری خدمات تضمین نمی‌شود. منابع به‌روز در README آمده‌اند.
- افزودن شهر، زمان‌های واقعی دوره‌ها و مشخصات توافق‌شده پلن‌ها، در صورت تمایل.
- اضافه‌کردن فایل ویدیو و جفت واقعی قبل/بعد؛ پشتیبانی کد موجود است ولی نمونه ساختگی در گالری قرار نگرفته است.
- نظرات مشتریان تا زمان ورود نظر واقعی پنهان‌اند.

داده‌های آزمایشی محلی در پوشه `.data` هستند و در Git یا فایل ZIP تحویل قرار نمی‌گیرند. هیچ ارسال پیام واقعی به مشتری، پیامک یا پرداخت انجام نشده است.
